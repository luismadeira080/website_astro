---
import SEO from '../../components/SEO.astro';
import Schema from '../../components/Schema.astro';
import LanguageSwitcher from '../../components/LanguageSwitcher.astro';
import Footer from '../../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="pt">
<head>
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-WHTQCRZ4');</script>
    <!-- End Google Tag Manager -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

    <!-- SEO Meta Tags -->
    <SEO
        title="Websites Profissionais - Luís Madeira"
        description="Websites modernos que aparecem no Google e convertem visitantes em clientes. Design limpo, rápido e otimizado para negócios locais."
        locale="pt"
    />

    <!-- Structured Data -->
    <Schema type="Service" />
</head>
<body>
    <!-- Navigation -->
    <nav class="navigation">
        <a href="/pt" class="logo">luís madeira</a>
        <div class="nav-links">
            <a href="/pt/#work">Trabalhos</a>
            <div class="nav-dropdown">
                <button class="nav-dropdown-toggle">
                    Serviços
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M7 10L12 15L17 10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="nav-dropdown-menu">
                    <a href="/pt/consultoria">AI Solutions</a>
                    <a href="/pt/servicos">Websites</a>
                </div>
            </div>
            <a href="/pt/sobre">Sobre</a>
            <a href="/pt/#contact">Contacto</a>
            <LanguageSwitcher currentPath="/pt/servicos" />
        </div>
        <div class="mobile-menu-toggle">
            <span></span>
            <span></span>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="page-hero">
        <div class="page-hero-content">
            <h1>WEBSITES PROFISSIONAIS</h1>
            <p class="page-hero-subtitle">Websites modernos que aparecem no Google e convertem visitantes em clientes.</p>
        </div>
    </section>

    <!-- Main Content -->
    <section class="content-section">
        <div class="content-wrapper">
            <h2>O Que Ofereço</h2>

            <div class="service-options">
                <div class="option-card">
                    <h3>Landing Pages</h3>
                    <p>Páginas simples e focadas, ideais para campanhas específicas, promoções ou quando precisa de algo rápido.</p>
                    <ul>
                        <li>Design limpo e moderno</li>
                        <li>Otimizado para conversões</li>
                        <li>Rápido de carregar</li>
                        <li>Formulário de contacto integrado</li>
                    </ul>
                    <div class="option-timing">
                        <strong>Prazo:</strong> 1-2 semanas
                    </div>
                    <a href="/pt/landing-page" class="option-cta">Saber Mais</a>
                </div>

                <div class="option-card featured">
                    <div class="featured-badge">Mais Popular</div>
                    <h3>Websites Completos com SEO</h3>
                    <p>Websites multi-página construídos para rankear no Google e atrair clientes locais através de pesquisa orgânica.</p>
                    <ul>
                        <li>Design profissional e responsivo</li>
                        <li>Múltiplas páginas otimizadas</li>
                        <li>SEO técnico completo</li>
                        <li>Configuração Google Analytics</li>
                        <li>Conteúdo otimizado para pesquisa local</li>
                        <li>Schema markup implementado</li>
                    </ul>
                    <div class="option-timing">
                        <strong>Prazo:</strong> 2-3 semanas
                    </div>
                    <a href="/pt/website-seo" class="option-cta">Saber Mais</a>
                </div>
            </div>

            <!-- Case Study Highlight -->
            <div class="case-study-highlight">
                <div class="case-study-content">
                    <div class="case-study-label">Caso de Estudo</div>
                    <h2>Porto Car Care</h2>
                    <p class="case-study-description">Oficina automóvel no Porto que alcançou resultados excepcionais com SEO local em apenas 3 meses.</p>
                    <div class="case-study-results">
                        <div class="result-item">
                            <div class="result-number">#1</div>
                            <div class="result-label">Posição no Google</div>
                        </div>
                        <div class="result-item">
                            <div class="result-number">ChatGPT</div>
                            <div class="result-label">Aparece no ChatGPT</div>
                        </div>
                        <div class="result-item">
                            <div class="result-number">3x</div>
                            <div class="result-label">Mais Chamadas</div>
                        </div>
                    </div>
                    <a href="/pt/projetos/porto-car-care" class="case-study-cta">Ver Caso de Estudo Completo</a>
                </div>
                <div class="case-study-image">
                    <img src="/final fnal final.svg" alt="Porto Car Care Website">
                </div>
            </div>

            <div class="work-examples">
                <h2>Outros Projetos</h2>
                <p>Veja mais projetos na <a href="/pt/projetos" class="inline-link">página de projetos</a>. Incluem websites para spas, clínicas dentárias e outros negócios locais em Portugal.</p>
            </div>
        </div>
    </section>

    <!-- How it Works Section -->
    <section class="how-it-works-section">
        <h2>COMO FUNCIONA</h2>
        <div class="process-grid">
            <div class="process-step">
                <div class="step-number">01</div>
                <h3>Descoberta</h3>
                <p>Discutimos os seus objetivos de negócio, público-alvo e concorrentes. Analiso o seu mercado e identifico as oportunidades que vão gerar resultados.</p>
            </div>
            <div class="process-step">
                <div class="step-number">02</div>
                <h3>Estratégia</h3>
                <p>Crio um plano detalhado para o seu website, incluindo direção de design, estrutura de conteúdo, estratégia de palavras-chave e métricas de sucesso.</p>
            </div>
            <div class="process-step">
                <div class="step-number">03</div>
                <h3>Desenvolvimento</h3>
                <p>Desenho e construo o seu site usando tecnologias web modernas. Verá atualizações de progresso e poderá dar feedback durante todo o desenvolvimento.</p>
            </div>
            <div class="process-step">
                <div class="step-number">04</div>
                <h3>Lançamento</h3>
                <p>O seu site fica online com todos os detalhes técnicos no lugar. Trato da configuração do domínio, integração de analytics e garanto que tudo funciona perfeitamente em todos os dispositivos.</p>
            </div>
            <div class="process-step">
                <div class="step-number">05</div>
                <h3>Otimização</h3>
                <p>Para websites SEO, monitorizo o desempenho durante 90 dias com melhorias contínuas. Receberá relatórios mensais sobre tráfego, rankings e métricas de conversão.</p>
            </div>
        </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
        <h2>PERGUNTAS FREQUENTES</h2>
        <div class="faq-grid">
            <div class="faq-item">
                <button class="faq-question">
                    <span>Quanto tempo demora a ver resultados de SEO?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>O tempo para ver resultados de SEO depende da concorrência local do seu setor. Em mercados menos competitivos, pode começar a ver melhorias nas primeiras duas semanas. Em mercados mais competitivos, pode demorar alguns meses. As landing pages podem começar a gerar tráfego imediatamente com anúncios pagos.</p>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span>Fornece suporte contínuo?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>Sim. Para websites otimizados para SEO, ofereço pacotes de otimização contínua. Se escolher um plano contínuo, forneço suporte básico e posso ajudar com atualizações conforme necessário.</p>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span>Posso atualizar o website eu mesmo?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>Atualizações diretas não são possíveis. Os meus sites usam código estático para as alterações que precisa. Para atualizações simples de conteúdo (texto, imagens, informação), não há custos adicionais. Alterações complexas que envolvam design ou funcionalidade requerem um orçamento separado.</p>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span>Trabalha com negócios fora de Portugal?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>Embora trabalhe principalmente com negócios portugueses, posso trabalhar com clientes internacionais. A minha especialização em SEO está focada em palavras-chave em português europeu e pesquisa local.</p>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span>E se eu já tiver um website?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>Posso auditar o seu site existente e recomendar melhorias, ou construir um novo de raiz se isso for mais económico. Vamos discutir a melhor abordagem após rever a sua situação atual.</p>
                </div>
            </div>

            <div class="faq-item">
                <button class="faq-question">
                    <span>Com que setores trabalha?</span>
                    <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <div class="faq-answer">
                    <p>Trabalho com vários negócios locais, incluindo clínicas médicas, restaurantes, spas, serviços automóveis, serviços profissionais, e mais. Se serve clientes numa localização específica, posso ajudar.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section id="contact" class="contact-section-simple">
        <div class="contact-container">
            <div class="cta-content">
                <h2>Pronto para Começar o Seu Projeto?</h2>
                <p class="cta-subtext">Websites modernos que aparecem no Google e convertem visitantes em clientes.</p>
                <a href="mailto:luismadeira@ruimadeira.pt" class="cta-button">Vamos Conversar</a>
            </div>
            <div class="cta-background-text">LM</div>
        </div>
    </section>

    <script>
        // Mobile Menu Toggle
        const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
        const navLinks = document.querySelector('.nav-links');

        if (mobileMenuToggle) {
            mobileMenuToggle.addEventListener('click', () => {
                mobileMenuToggle.classList.toggle('active');
                navLinks.classList.toggle('active');
            });

            // Close menu when clicking on a link
            const links = navLinks.querySelectorAll('a');
            links.forEach(link => {
                link.addEventListener('click', () => {
                    mobileMenuToggle.classList.remove('active');
                    navLinks.classList.remove('active');
                });
            });
        }

        // Dropdown navigation
        const dropdownToggle = document.querySelector('.nav-dropdown-toggle');
        const dropdownMenu = document.querySelector('.nav-dropdown-menu');
        const navDropdown = document.querySelector('.nav-dropdown');

        if (dropdownToggle && dropdownMenu) {
            dropdownToggle.addEventListener('click', (e) => {
                e.stopPropagation();
                navDropdown.classList.toggle('active');
            });

            // Close dropdown when clicking outside
            document.addEventListener('click', () => {
                navDropdown.classList.remove('active');
            });

            navDropdown.addEventListener('click', (e) => {
                e.stopPropagation();
            });
        }

        // FAQ Accordion
        const faqQuestions = document.querySelectorAll('.faq-question');
        faqQuestions.forEach(question => {
            question.addEventListener('click', () => {
                const faqItem = question.parentElement;
                const isOpen = faqItem.classList.contains('open');

                // Close all other FAQ items
                document.querySelectorAll('.faq-item').forEach(item => {
                    item.classList.remove('open');
                });

                // Toggle current item
                if (!isOpen) {
                    faqItem.classList.add('open');
                }
            });
        });
    </script>

    <Footer locale="pt" />
</body>
</html>

<style>
/* Reset & Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --color-bg: #0d0d0d;
    --color-text: #c7c7c7;
    --color-accent: #d3e97a;
    --color-dark: #1a1a1a;
    --font-primary: 'Inter', sans-serif;
    --font-heading: 'Bebas Neue', sans-serif;
}

body {
    font-family: var(--font-primary);
    background-color: var(--color-bg);
    color: var(--color-text);
    line-height: 1.6;
    overflow-x: hidden;
}

/* Navigation */
.navigation {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px 60px;
    position: sticky;
    top: 0;
    background-color: var(--color-bg);
    z-index: 1000;
    transition: transform 0.3s ease;
}

.logo {
    font-family: var(--font-heading);
    font-size: 32px;
    letter-spacing: -0.32px;
    color: var(--color-text);
    text-decoration: none;
}

.nav-links {
    display: flex;
    gap: 32px;
    align-items: center;
}

.nav-links a {
    color: var(--color-text);
    text-decoration: none;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: -0.48px;
    transition: color 0.3s ease;
}

.nav-links a:hover {
    color: var(--color-accent);
}

/* Dropdown Navigation */
.nav-dropdown {
    position: relative;
}

.nav-dropdown-toggle {
    display: flex;
    align-items: center;
    gap: 6px;
    background: none;
    border: none;
    color: var(--color-text);
    font-size: 16px;
    font-weight: 500;
    letter-spacing: -0.48px;
    cursor: pointer;
    padding: 0;
    font-family: var(--font-primary);
    transition: color 0.3s ease;
}

.nav-dropdown-toggle:hover {
    color: var(--color-accent);
}

.nav-dropdown-toggle svg {
    transition: transform 0.3s ease;
}

.nav-dropdown.active .nav-dropdown-toggle svg {
    transform: rotate(180deg);
}

.nav-dropdown-menu {
    position: absolute;
    top: calc(100% + 16px);
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--color-dark);
    border: 1px solid #333;
    border-radius: 12px;
    padding: 8px;
    min-width: 160px;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
    z-index: 1000;
}

.nav-dropdown.active .nav-dropdown-menu {
    opacity: 1;
    visibility: visible;
}

.nav-dropdown-menu a {
    display: block;
    padding: 12px 16px;
    color: var(--color-text);
    text-decoration: none;
    font-size: 15px;
    border-radius: 8px;
    transition: background-color 0.2s ease, color 0.2s ease;
}

.nav-dropdown-menu a:hover {
    background-color: rgba(211, 233, 122, 0.1);
    color: var(--color-accent);
}

.mobile-menu-toggle {
    display: none;
    flex-direction: column;
    gap: 8px;
    cursor: pointer;
}

.mobile-menu-toggle span {
    width: 32px;
    height: 2px;
    background-color: var(--color-text);
    transition: all 0.3s ease;
}

/* Page Hero */
.page-hero {
    padding: 120px 108px 80px;
    border-bottom: 1px solid #333;
}

.page-hero-content {
    max-width: 900px;
}

.page-hero h1 {
    font-family: var(--font-heading);
    font-size: 76px;
    line-height: 1.1;
    letter-spacing: -0.76px;
    color: var(--color-text);
    margin-bottom: 24px;
}

.page-hero-subtitle {
    font-size: 24px;
    line-height: 1.4;
    color: var(--color-text);
    opacity: 0.9;
}

/* Content Section */
.content-section {
    padding: 80px 108px;
}

.content-wrapper {
    max-width: 1200px;
    margin: 0 auto;
}

.content-wrapper h2 {
    font-family: var(--font-heading);
    font-size: 36px;
    letter-spacing: -0.36px;
    margin-top: 0;
    margin-bottom: 40px;
    color: var(--color-text);
}

.work-examples {
    margin-top: 64px;
    text-align: center;
}

.work-examples h2 {
    margin-bottom: 16px;
}

.work-examples p {
    font-size: 17px;
    line-height: 1.7;
    opacity: 0.85;
}

.inline-link {
    color: var(--color-accent);
    text-decoration: none;
    border-bottom: 1px solid var(--color-accent);
    transition: opacity 0.2s ease;
}

.inline-link:hover {
    opacity: 0.7;
}

/* Service Options */
.service-options {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
    margin: 40px 0;
}

.option-card {
    background-color: var(--color-dark);
    border: 1px solid #333;
    border-radius: 16px;
    padding: 32px;
    position: relative;
}

.option-card.featured {
    border-color: var(--color-accent);
    background-color: rgba(211, 233, 122, 0.03);
}

.featured-badge {
    position: absolute;
    top: -12px;
    right: 24px;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 6px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.option-card h3 {
    font-size: 24px;
    margin-bottom: 16px;
    color: var(--color-text);
}

.option-card p {
    font-size: 16px;
    line-height: 1.6;
    margin-bottom: 20px;
    opacity: 0.9;
}

.option-card ul {
    list-style: none;
    padding: 0;
    margin: 24px 0;
}

.option-card li {
    padding-left: 24px;
    position: relative;
    margin-bottom: 12px;
    font-size: 15px;
    line-height: 1.6;
}

.option-card li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 600;
}

.option-timing {
    margin-top: 24px;
    padding-top: 24px;
    border-top: 1px solid #333;
    font-size: 14px;
    opacity: 0.8;
}

.option-cta {
    display: block;
    text-align: center;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 14px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 15px;
    transition: transform 0.2s ease, background-color 0.2s ease;
    margin-top: 24px;
}

.option-cta:hover {
    background-color: #c5db6f;
    transform: translateY(-2px);
}

/* Sidebar */
.content-sidebar {
    position: sticky;
    top: 96px;
    align-self: start;
}

.sidebar-card {
    background-color: var(--color-dark);
    border: 1px solid #333;
    border-radius: 16px;
    padding: 32px;
    margin-bottom: 24px;
}

.sidebar-card h3 {
    font-size: 20px;
    margin-bottom: 20px;
    color: var(--color-text);
}

.sidebar-card p {
    font-size: 15px;
    line-height: 1.6;
    margin-bottom: 16px;
    opacity: 0.9;
}

.sidebar-card p:last-child {
    margin-bottom: 0;
}

.sidebar-card ul {
    list-style: none;
    padding: 0;
}

.sidebar-card li {
    padding-left: 24px;
    position: relative;
    margin-bottom: 12px;
    font-size: 15px;
    line-height: 1.6;
}

.sidebar-card li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 600;
}

.cta-card {
    background-color: rgba(211, 233, 122, 0.05);
    border-color: var(--color-accent);
}

.sidebar-cta {
    display: block;
    text-align: center;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 14px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 15px;
    transition: transform 0.2s ease, background-color 0.2s ease;
    margin-top: 20px;
}

.sidebar-cta:hover {
    background-color: #c5db6f;
    transform: translateY(-2px);
}

.sidebar-cta-secondary {
    display: block;
    text-align: center;
    color: var(--color-accent);
    padding: 12px 28px;
    border: 1px solid var(--color-accent);
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    font-size: 15px;
    transition: background-color 0.2s ease;
    margin-top: 12px;
}

.sidebar-cta-secondary:hover {
    background-color: rgba(211, 233, 122, 0.1);
}

/* How it Works Section */
.how-it-works-section {
    padding: 100px 108px;
    background-color: var(--color-bg);
    border-top: 1px solid #333;
}

.how-it-works-section h2 {
    font-family: var(--font-heading);
    font-size: 64px;
    letter-spacing: -0.64px;
    text-align: center;
    margin-bottom: 80px;
    color: var(--color-text);
}

.process-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 32px;
}

.process-step {
    text-align: left;
}

.step-number {
    font-family: var(--font-heading);
    font-size: 56px;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 16px;
}

.process-step h3 {
    font-size: 20px;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 12px;
}

.process-step p {
    font-size: 14px;
    line-height: 1.6;
    color: var(--color-text);
    opacity: 0.85;
}

/* FAQ Section */
.faq-section {
    padding: 100px 108px;
    background-color: var(--color-bg);
    border-top: 1px solid #333;
}

.faq-section h2 {
    font-family: var(--font-heading);
    font-size: 64px;
    letter-spacing: -0.64px;
    text-align: center;
    margin-bottom: 80px;
    color: var(--color-text);
}

.faq-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
    max-width: 1200px;
    margin: 0 auto;
}

.faq-item {
    border-bottom: 1px solid #333;
}

.faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px 0;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    color: var(--color-text);
    font-family: var(--font-primary);
    font-size: 16px;
    font-weight: 600;
    transition: color 0.3s ease;
}

.faq-question:hover {
    color: var(--color-accent);
}

.faq-icon {
    flex-shrink: 0;
    transition: transform 0.3s ease;
    stroke: var(--color-text);
}

.faq-item.open .faq-icon {
    transform: rotate(180deg);
}

.faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, padding 0.3s ease;
}

.faq-item.open .faq-answer {
    max-height: 300px;
    padding-bottom: 24px;
}

.faq-answer p {
    font-size: 15px;
    line-height: 1.7;
    color: var(--color-text);
    opacity: 0.85;
    margin: 0;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .navigation {
        padding: 24px 32px;
    }

    .page-hero {
        padding: 80px 32px 60px;
    }

    .content-section {
        padding: 60px 32px;
    }

    .content-wrapper {
        grid-template-columns: 1fr;
        gap: 48px;
    }

    .service-options {
        grid-template-columns: 1fr;
    }

    .content-sidebar {
        position: static;
    }

    .how-it-works-section {
        padding: 60px 32px;
    }

    .how-it-works-section h2 {
        font-size: 48px;
    }

    .process-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 48px;
    }

    .faq-section {
        padding: 60px 32px;
    }

    .faq-section h2 {
        font-size: 48px;
    }

    .faq-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .navigation {
        padding: 20px 16px;
    }

    .logo {
        font-size: 28px;
    }

    .nav-links {
        position: fixed;
        top: 76px;
        right: -100%;
        width: 100%;
        height: calc(100vh - 76px);
        background-color: var(--color-bg);
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        padding: 40px 0;
        transition: right 0.3s ease;
        border-top: 1px solid #333;
    }

    .nav-links.active {
        right: 0;
    }

    .nav-links a {
        font-size: 24px;
    }

    .nav-dropdown-toggle {
        font-size: 24px;
    }

    .nav-dropdown-menu {
        position: static;
        transform: none;
        margin-top: 16px;
        border: none;
        background-color: transparent;
        padding: 0;
    }

    .nav-dropdown.active .nav-dropdown-menu {
        display: block;
    }

    .nav-dropdown-menu:not(.active) {
        display: none;
    }

    .nav-dropdown-menu a {
        font-size: 20px;
        padding: 12px 0;
    }

    .mobile-menu-toggle {
        display: flex;
    }

    .mobile-menu-toggle.active span:first-child {
        transform: rotate(45deg) translate(8px, 8px);
    }

    .mobile-menu-toggle.active span:last-child {
        transform: rotate(-45deg) translate(8px, -8px);
    }

    .page-hero {
        padding: 60px 16px 40px;
    }

    .page-hero h1 {
        font-size: 48px;
    }

    .page-hero-subtitle {
        font-size: 18px;
    }

    .content-section {
        padding: 40px 16px;
    }

    .how-it-works-section {
        padding: 40px 16px;
    }

    .how-it-works-section h2 {
        font-size: 40px;
        margin-bottom: 48px;
    }

    .process-grid {
        grid-template-columns: 1fr;
        gap: 40px;
    }

    .step-number {
        font-size: 48px;
    }

    .faq-section {
        padding: 40px 16px;
    }

    .faq-section h2 {
        font-size: 40px;
        margin-bottom: 48px;
    }

    .faq-question {
        font-size: 15px;
        padding: 20px 0;
    }

    .faq-answer p {
        font-size: 14px;
    }
}

/* Case Study Highlight */
.case-study-highlight {
    margin-top: 80px;
    background: linear-gradient(135deg, #1a1a1a 0%, #252525 100%);
    border: 2px solid var(--color-accent);
    border-radius: 24px;
    padding: 60px;
    display: grid;
    grid-template-columns: 1fr 500px;
    gap: 60px;
    align-items: center;
}

.case-study-content {
    position: relative;
}

.case-study-label {
    display: inline-block;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 6px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 24px;
}

.case-study-highlight h2 {
    font-family: var(--font-heading);
    font-size: 56px;
    letter-spacing: -0.56px;
    margin-bottom: 16px;
    color: #ffffff;
}

.case-study-description {
    font-size: 18px;
    line-height: 1.6;
    margin-bottom: 40px;
    color: rgba(255, 255, 255, 0.85);
}

.case-study-results {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-bottom: 40px;
}

.result-item {
    text-align: center;
}

.result-number {
    font-family: var(--font-heading);
    font-size: 48px;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 8px;
}

.result-label {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.4;
}

.case-study-cta {
    display: inline-flex;
    align-items: center;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 16px 32px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 15px;
    transition: transform 0.2s ease, background-color 0.2s ease;
}

.case-study-cta:hover {
    background-color: #c5db6f;
    transform: translateY(-2px);
}

.case-study-image {
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.case-study-image img {
    width: 100%;
    height: auto;
    display: block;
}

/* CTA Section */
.contact-section-simple {
    padding: 0;
    margin: 80px 108px;
    position: relative;
    overflow: hidden;
}

.contact-container {
    background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
    border: 2px solid var(--color-accent);
    border-radius: 32px;
    padding: 120px 80px;
    position: relative;
    overflow: hidden;
}

.cta-content {
    position: relative;
    z-index: 2;
    max-width: 700px;
}

.contact-section-simple h2 {
    font-family: var(--font-heading);
    font-size: 72px;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -1.5px;
    margin-bottom: 32px;
    color: #ffffff;
}

.cta-subtext {
    font-size: 18px;
    line-height: 1.6;
    margin-bottom: 48px;
    color: rgba(255, 255, 255, 0.9);
    max-width: 600px;
}

.cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-accent);
    color: var(--color-bg);
    padding: 18px 48px;
    border-radius: 12px;
    border: none;
    font-weight: 600;
    font-size: 16px;
    letter-spacing: 0.3px;
    cursor: pointer;
    transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
    font-family: var(--font-primary);
    text-decoration: none;
}

.cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(211, 233, 122, 0.4);
    background-color: #c5db6f;
}

.cta-background-text {
    position: absolute;
    right: 80px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 400px;
    font-weight: 900;
    color: rgba(211, 233, 122, 0.05);
    line-height: 1;
    font-family: var(--font-heading);
    pointer-events: none;
    user-select: none;
}

/* Responsive Case Study & CTA */
@media (max-width: 1024px) {
    .case-study-highlight {
        grid-template-columns: 1fr;
        padding: 48px;
        margin-top: 60px;
    }

    .case-study-image {
        order: -1;
    }

    .case-study-highlight h2 {
        font-size: 48px;
    }

    .result-number {
        font-size: 40px;
    }

    .contact-section-simple {
        margin: 60px 32px;
    }

    .contact-container {
        padding: 80px 48px;
    }

    .cta-background-text {
        font-size: 300px;
        right: 40px;
    }
}

@media (max-width: 768px) {
    .case-study-highlight {
        padding: 32px 24px;
        margin-top: 48px;
        border-radius: 16px;
    }

    .case-study-highlight h2 {
        font-size: 40px;
        margin-bottom: 12px;
    }

    .case-study-description {
        font-size: 16px;
        margin-bottom: 32px;
    }

    .case-study-results {
        grid-template-columns: 1fr;
        gap: 24px;
        margin-bottom: 32px;
    }

    .result-number {
        font-size: 36px;
    }

    .result-label {
        font-size: 13px;
    }

    .case-study-cta {
        width: 100%;
        justify-content: center;
    }

    .contact-section-simple {
        margin: 40px 16px;
    }

    .contact-container {
        padding: 60px 32px;
        border-radius: 24px;
    }

    .contact-section-simple h2 {
        font-size: 40px;
        letter-spacing: -1px;
        margin-bottom: 24px;
    }

    .cta-subtext {
        font-size: 16px;
        margin-bottom: 32px;
    }

    .cta-button {
        padding: 16px 40px;
        font-size: 15px;
    }

    .cta-background-text {
        display: none;
    }
}
</style>
